/** @type {import('next').NextConfig} */
const nextConfig = {
  /*webpack: (config,options) => {
    if(!options.isServer) {
      config.devtool = options.isServer ? 'source-map':false;
    }
    return config;
  },*/
}

module.exports = nextConfig
